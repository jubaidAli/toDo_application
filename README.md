# toDo_application

Monorepo with `frontend` (React) and `backend` (Node/Express) for a todo app.

## How to run

- Backend: cd backend && npm install && npm run dev
- Frontend: cd frontend && npm install && npm start
